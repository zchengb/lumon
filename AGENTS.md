# Lumon Agent Workflow

本文件记录 Lumon 项目在实现、验证和发布阶段的协作约定。

## 变更完成后的默认行为

1. 完成实现后先运行与变更相关的测试和质量门禁。
2. 未明确要求发布时，不自动升级版本号、创建 commit 或 push。
3. 验证通过后，在对话中一次性说明验证结果，并询问是否执行“升级版本号 + commit + push”。
4. 不依赖操作系统弹窗；确认通过当前 Agent 对话完成。仓库说明不能绕过系统或用户授权规则。

## 用户明确要求发布时

当用户明确要求“提交并推送”或等价表达时，按以下顺序执行：

1. 根据变更类型升级 `src/lumon/version.py` 中的版本号；兼容性改动默认递增 patch 版本。
2. 同步更新必要的版本断言、发布说明和测试，不修改无关文件。
3. 运行前端 typecheck/test/build、Python pytest/ruff/pyright、静态资源检查和 `uv build`。
4. 只暂存本次变更，检查 staged diff 中没有凭据、临时文件或意外生成物。
5. 按 Conventional Commits 创建本地 commit。
6. 版本号与 tag 一致后创建 `vX.Y.Z` tag，并 push 分支和 tag。
7. 跟踪 GitHub Actions，确认 GitHub Release 与包产物成功生成。

如果用户只要求 commit，则在第 5 步结束；如果只要求 push，则先确认已有待推送 commit 和发布目标，不替用户创建无关 commit。

## Lumon 发布约定

- 主开发分支：`release`。
- Python 包版本唯一来源：`src/lumon/version.py`。
- Release tag 必须使用 `vX.Y.Z`，并与包版本完全一致。
- GitHub Actions 的 release workflow 由版本 tag 触发。
- 发布包只存储在 GitHub Release/Actions artifact，不发布到公共包索引。
- 发布后升级入口为 `lumon update`；升级不修改 Workspace。
- 不把 token、密码、Webhook 或 SSH 私钥写入仓库、日志、commit 或命令输出。

## Python 工程约定

本节约束新增和修改的 Python 代码。不要因为本节约定而顺手重构未触及的旧代码；如果发现旧代码需要治理，记录为后续任务即可。

### 可读性优先

- 代码应当按业务步骤从上到下阅读；优先使用有意义的中间变量和 guard clause，不要把多个业务动作压缩到一行。
- 一个函数尽量只表达一个业务动作。函数同时负责解析、校验、持久化、调用外部服务和格式化响应时，应优先拆出清晰的阶段或小对象。
- 新增函数以 McCabe 圈复杂度不超过 10、分支不超过 12、语句不超过 50 为设计目标。超过目标不是机械拆函数的理由，但必须能说明复杂度来自哪里，并优先降低嵌套和职责混杂。
- 避免嵌套三元表达式、过长的布尔表达式、难以调试的链式调用、为了少写几行而使用的“聪明”推导式，以及隐式的全局状态。
- 变量名应表达业务含义；在模块边界内不要让 `data`、`value`、`result`、`obj` 等泛化名称承担多个不同含义。
- 注释解释原因、约束和不变量，不重复代码本身。复杂流程应通过命名和结构变清晰，而不是堆叠注释。

### 模块边界和依赖方向

- 依赖方向保持单向：领域/策略代码不依赖 CLI、Dashboard、具体 SDK 或基础设施实现；外部系统通过 adapter、gateway 或 runner 接入。
- 只有在存在真实替换点（外部服务、文件系统、时钟、进程、数据库，或需要 fake 的测试边界）时才引入 `Protocol`；不要为每个类机械地创建接口。
- 核心逻辑不得偷偷读取环境变量、当前时间、用户目录、网络或进程输出。把这些副作用放在组合根或 adapter，并通过参数或窄接口注入。
- 模块接口应窄而明确：调用方只依赖它实际需要的方法；跨边界传递 dataclass、`TypedDict` 或明确的值对象，不要让未验证的原始 `dict` 在系统中流动。
- 配置、命令和结果优先使用不可变、带类型的数据结构；参数较多时使用 keyword-only 参数或命令对象，避免调用方依赖参数位置。

### 类型、错误和安全

- 新增和修改代码必须保持 Pyright strict 通过；公共函数、边界函数和异步函数都要有完整类型标注。
- `Any` 只允许出现在第三方 SDK 或动态输入的最外层边界，并应尽快转换为本地类型。不要用无依据的 `cast` 掩盖类型问题。
- 有限状态优先使用 `Enum`/`StrEnum` 或 `Literal`，不要用多个互相约束的布尔值表达状态机。
- 捕获具体异常；`except Exception` 只允许位于进程、任务或外部适配器等最终边界，并且要记录安全上下文、保留异常链（`raise ... from exc`），不能静默吞错。
- 错误信息和日志不得包含 token、密码、Webhook、完整 prompt、用户隐私或其他凭据。外部输入在进入核心逻辑前完成校验、归一化和脱敏。
- 不为了“看起来健壮”而在每一层重复 try/except、重试或 fallback。重试、超时、幂等和回滚应由明确的边界负责，并有测试覆盖。

### 测试和验证

- 每个新增行为至少有一个针对公开接口或业务结果的测试；优先使用 fake/fixture 注入依赖，不访问真实网络、账号、用户目录或生产数据。
- 重点测试边界条件、失败路径、重试/超时、幂等性、事务回滚和部分成功，而不只测试 happy path。
- 测试实现行为而非私有方法的当前形状。只有在私有逻辑本身是稳定且高风险的协议时，才直接测试私有方法。
- Python 变更完成后至少运行相关测试；合并前运行：
  - `.venv/bin/ruff format --check .`
  - `.venv/bin/ruff check .`
  - `.venv/bin/pyright --pythonpath .venv/bin/python`
  - `.venv/bin/python -m pytest`
- 如果环境没有 `.venv` 或某项无法运行，要明确说明原因，不要把“未运行”写成“通过”。

### 修改边界

- 开始编码前先读相关模块、测试、配置和本文件；先写出目标、不变量、依赖变化和验证方式，再修改代码。
- 采用最小可审查改动：不顺手改名、格式化、升级依赖或重排无关文件；不以“顺便清理”为理由扩大 diff。
- 新功能优先复用已有 seam 和类型，不要复制第二套异常、配置、持久化或外部调用逻辑。
- 完成后报告修改文件、行为变化、验证命令、已知风险和未处理的后续建议。除非用户明确要求，不创建 commit、不 push、不升级版本。