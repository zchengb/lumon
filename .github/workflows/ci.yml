name: Lumon CI

on:
  pull_request:
    branches: [release]
  push:
    branches: [release]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-and-build:
    name: Test and build
    runs-on: ubuntu-latest

    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: dashboard-ui/package-lock.json

      - name: Install frontend dependencies
        run: npm --prefix dashboard-ui ci

      - name: Check frontend types
        run: npm --prefix dashboard-ui run typecheck

      - name: Test frontend
        run: npm --prefix dashboard-ui run test

      - name: Build frontend
        run: npm --prefix dashboard-ui run build

      - name: Install Python
        run: uv python install 3.12

      - name: Install locked development environment
        run: uv sync --locked --dev

      - name: Verify Dashboard assets
        run: uv run python tools/check_dashboard_assets.py

      - name: Ruff check
        run: uv run ruff check

      - name: Ruff format check
        run: uv run ruff format --check

      - name: Pyright
        run: uv run pyright --pythonpath .venv/bin/python

      - name: Pytest
        run: uv run pytest

      - name: Build distributions
        run: uv build

      - name: Create distribution checksums
        run: uv run python tools/create_checksums.py dist

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: lumon-ci-${{ github.sha }}
          path: dist/
          if-no-files-found: error
          retention-days: 14
