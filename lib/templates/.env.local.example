# Local secrets and machine-specific values.
# Copy this file to .env.local and fill in values locally.
# .env.local must never be committed.

FEISHU_WEBHOOK_URL=""
CURSOR_API_KEY=""
DEEPSEEK_API_KEY=""
# OPENAI_API_KEY=""

# Dylan Feishu self-built app (Agent Gateway). Never commit real secrets.
# FEISHU_DYLAN_APP_ID=""
# FEISHU_DYLAN_APP_SECRET=""

# Optional: only needed when a non-interactive scheduled job cannot use an
# existing `gh auth login` session to create pull requests.
# GH_HOST=""
# GH_TOKEN=""

# Authenticated Web Delivery credentials are saved here by repository env name:
#   lumen config set-visual-auth <repository> <credential> --project <slug>
# Optional Playwright storage state for auth_strategy=storage-state:
# LUMEN_WEB_STORAGE_STATE="/absolute/path/to/storage-state.json"
